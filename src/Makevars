# Makevars for Linux/macOS
PKG_CPPFLAGS = $(SHLIB_OPENMP_CXXFLAGS) $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::CxxFlags()")
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()")
CXX_STD = CXX11
